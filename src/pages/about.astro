---
import Layout from '../layouts/Layout.astro';

const principles = [
	{ title: 'Clarity first', body: 'Small docs, tight scopes, and weekly demos keep risk visible.' },
	{ title: 'Operable by default', body: 'Logging, monitoring, and runbooks land with every build.' },
	{ title: 'Boring is better', body: 'We choose dependable stacks over shiny experiments.' },
];
---

<Layout
	title="About — G&K Software, LLC"
	description="Texas-registered software studio founded by Gibson Martin and Kyle Dougan."
>
	<main class="page about">
		<nav class="about-nav">
			<a class="ghost-link" href="/">Home</a>
			<a class="ghost-link" href="/#services">Services</a>
			<a class="ghost-link" href="/#contact">Contact</a>
		</nav>

		<section class="intro">
			<p class="eyebrow">About G&K Software, LLC</p>
			<h1>Two founders, building with you—not over you.</h1>
			<p class="lede">
				Gibson Martin and Kyle Dougan design and ship web products end-to-end. We’re registered in
				Texas and work with distributed teams across North America and Europe.
			</p>
			<div class="callout">
				<p class="muted">
					We stay founder-led. You get direct access to the people writing code, making architectural
					calls, and presenting every demo.
				</p>
			</div>
		</section>

		<section class="grid founders">
			<article class="panel">
				<div class="pill">G</div>
				<div>
					<p class="eyebrow">Founder</p>
					<h3>Gibson Martin</h3>
					<p class="muted">
						Product-minded engineer who keeps experiments honest and ships iteratively. Loves
						stabilizing APIs and smoothing onboarding flows.
					</p>
				</div>
			</article>
			<article class="panel">
				<div class="pill">K</div>
				<div>
					<p class="eyebrow">Founder</p>
					<h3>Kyle Dougan</h3>
					<p class="muted">
						Systems thinker obsessed with observability and operational clarity. Builds tools that
						remove manual effort for ops, support, and finance teams.
					</p>
				</div>
			</article>
			<article class="panel accent">
				<div class="pill">TX</div>
				<div>
					<p class="eyebrow">Texas registered</p>
					<h3>Available remotely</h3>
					<p class="muted">
						We collaborate async or live depending on your team’s rhythm. Travel for key milestones
						is on the table when it makes sense.
					</p>
				</div>
			</article>
		</section>

		<section class="principles">
			<div class="section-head">
				<p class="eyebrow">Operating principles</p>
				<h2>Simple rules that keep projects moving.</h2>
			</div>
			<div class="principle-list">
				{principles.map((item, index) => (
					<div class="principle-card">
						<div class="badge">{index + 1}</div>
						<div>
							<p class="step-title">{item.title}</p>
							<p class="muted">{item.body}</p>
						</div>
					</div>
				))}
			</div>
		</section>

		<section class="cta">
			<div>
				<p class="eyebrow">Ready when you are</p>
				<h2>Let’s scope your next release.</h2>
				<p class="muted">
					Send a short note with your timelines. We’ll reply within a business day with a draft
					plan.
				</p>
			</div>
			<a class="btn primary" href="mailto:hello@gnksoftware.com">Email the founders</a>
		</section>
	</main>
</Layout>

<style>
	.page.about {
		max-width: 1100px;
		margin: 0 auto;
		padding: 32px 20px 96px;
		display: flex;
		flex-direction: column;
		gap: 24px;
		position: relative;
	}

	.about-nav {
		display: flex;
		gap: 12px;
		flex-wrap: wrap;
		border: 1px solid var(--stroke);
		border-radius: 12px;
		padding: 10px 12px;
		background: rgba(255, 255, 255, 0.02);
	}

	.ghost-link {
		color: var(--muted);
		text-decoration: none;
		font-weight: 600;
		transition: color 0.2s ease;
	}

	.ghost-link:hover {
		color: white;
	}

	.eyebrow {
		text-transform: uppercase;
		letter-spacing: 0.08em;
		font-size: 12px;
		color: var(--muted);
		margin: 0;
	}

	.muted {
		color: var(--muted);
		margin: 0;
		line-height: 1.6;
	}

	.intro {
		display: grid;
		gap: 12px;
	}

	.intro h1 {
		font-size: clamp(30px, 4vw, 46px);
		margin: 0;
		font-family: 'Space Grotesk', 'Manrope', system-ui, sans-serif;
	}

	.lede {
		color: var(--muted);
		line-height: 1.7;
		max-width: 820px;
	}

	.callout {
		border: 1px solid var(--stroke);
		border-radius: 16px;
		padding: 16px;
		background: linear-gradient(120deg, rgba(255, 180, 84, 0.08), rgba(127, 231, 211, 0.08));
	}

	.grid.founders {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 14px;
	}

	.panel {
		border: 1px solid var(--stroke);
		border-radius: 16px;
		background: rgba(255, 255, 255, 0.03);
		padding: 16px;
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 12px;
		align-items: center;
	}

	.panel.accent {
		background: linear-gradient(150deg, rgba(127, 231, 211, 0.12), rgba(255, 180, 84, 0.08));
	}

	.pill {
		width: 44px;
		height: 44px;
		border-radius: 12px;
		border: 1px solid var(--stroke);
		display: grid;
		place-items: center;
		font-weight: 800;
		font-family: 'Space Grotesk', 'Manrope', sans-serif;
		background: rgba(255, 255, 255, 0.08);
	}

	.section-head {
		margin-bottom: 4px;
	}

	h2 {
		margin: 6px 0 0;
	}

	.principles {
		display: grid;
		gap: 12px;
	}

	.principle-list {
		display: grid;
		gap: 10px;
	}

	.principle-card {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 10px;
		align-items: center;
		border: 1px solid var(--stroke);
		border-radius: 12px;
		padding: 12px;
		background: rgba(255, 255, 255, 0.02);
	}

	.badge {
		width: 32px;
		height: 32px;
		border-radius: 10px;
		display: grid;
		place-items: center;
		border: 1px solid var(--stroke);
		font-weight: 700;
	}

	.step-title {
		margin: 0 0 4px;
		font-weight: 700;
	}

	.btn {
		border-radius: 999px;
		padding: 12px 18px;
		text-decoration: none;
		font-weight: 700;
		border: 1px solid transparent;
		display: inline-flex;
		align-items: center;
		justify-content: center;
	}

	.btn.primary {
		background: linear-gradient(135deg, #ffb454, #ffd699);
		color: #0f162c;
		box-shadow: 0 8px 24px rgba(255, 180, 84, 0.35);
	}

	.cta {
		margin-top: 12px;
		border: 1px solid var(--stroke);
		border-radius: 16px;
		padding: 20px;
		background: rgba(255, 255, 255, 0.02);
		display: grid;
		grid-template-columns: 2fr auto;
		gap: 12px;
		align-items: center;
	}

	@media (max-width: 700px) {
		.cta {
			grid-template-columns: 1fr;
		}
	}
</style>
